sudo: false
os:
  - osx
  - linux
language: go
go:
  - 1.10.x
script:
  - env GO111MODULE=on go test -v tests -timeout 0 -rt.url="$JFROG_CLI_RT_URL" -rt.user="$JFROG_CLI_RT_USER" -rt.password="$JFROG_CLI_RT_PASSWORD"
