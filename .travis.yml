sudo: false
os:
  - osx
  - linux
language: go
env:
  - GO111MODULE=on
go:
  - 1.10.x
install: true
script:
  - go test -v tests -timeout 0 -rt.url="$JFROG_CLI_RT_URL" -rt.user="$JFROG_CLI_RT_USER" -rt.password="$JFROG_CLI_RT_PASSWORD"
